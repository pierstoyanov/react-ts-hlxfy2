import * as React from 'react';
import { useTranslation } from "react-i18next";

const Read = () => {
    const { t } = useTranslation();

    
    return (
        <>{t[""]}

        </>
    );
};

export default Read;
